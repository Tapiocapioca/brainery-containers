# Brainery Docker Ports Configuration
#
# Copy this file to .env to customize ports.
# If .env is missing, these default ports are used automatically.
#
# Default port range: 9100-9103 (IANA unassigned, low conflict probability)
# Ports are ordered by workflow: extract -> process -> store

# Web extraction (crawl4ai)
CRAWL4AI_PORT=9100

# Video extraction (yt-dlp-server)
YTDLP_PORT=9101

# Audio transcription (whisper-server)
WHISPER_PORT=9102

# RAG storage + UI (anythingllm)
ANYTHINGLLM_PORT=9103

# Unstructured API (Document Parsing)
UNSTRUCTURED_PORT=9104
UNSTRUCTURED_API_KEY=
